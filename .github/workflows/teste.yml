name: TESTE - RUNNER SSH

on:
  workflow_dispatch:

jobs:
  teste-conexao:
    runs-on: [self-hosted, Linux, X64]

    steps:
      - name: Test Runner
        run: |
           ssh -i "/home/ubuntu/.ssh/vm-ec2-default-key.pem" ubuntu@ec2-10-0-1-142.compute-1.amazonaws.com
           mkdir TETSE-SSH-FUNCIONOU

