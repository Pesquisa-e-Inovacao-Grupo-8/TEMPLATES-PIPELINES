name: TESTE - RUNNER SSH

on:
  workflow_dispatch:

jobs:
  teste-conexao:
    runs-on: [self-hosted, Linux, X64]
  secrets:
    EC2_PRIVATE_IP:
      description: 'IP privado da instância EC2'
      required: true

    steps:
      - name: Test Runner
        run: |
           ssh -i "/home/ubuntu/.ssh/vm-ec2-default-key.pem" ubuntu@ec2-${{ secrets.EC2_PRIVATE_IP }}.compute-1.amazonaws.com
           mkdir TETSE-SSH-FUNCIONOU

